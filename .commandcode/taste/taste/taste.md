# Taste
- Prefers to store and share all credentials/configuration via a `.env` file in the project (stated they would share all credentials in `.env`, explicitly directed "use these credentials for .env file" when supplying Jira/Groq keys, and again pointed the agent to the project `.env` file as the config source, telling it to "use this and proceed"), rather than hardcoding them in code. Confidence: 0.98
- Wants an explicit implementation plan written to a `Plan.md` file inside the project (e.g., `src/Plan.md`) before any code is created, so the plan is reviewable in-repo. Confidence: 0.8
- Wants completed work committed and pushed to the configured GitHub repository as part of finishing a task ("Commit and Push all source code in the repo"). Confidence: 0.85
- Wants the README.md kept updated to reflect the delivered code (features, project structure, setup/config notes) alongside the code itself. Confidence: 0.7
- Prefers reusing the helper toolkit/scripts the user provides for a task (e.g., `resume-helper` / `resume-tailor` skill) and following its documented workflow, rather than building new tooling from scratch ("use this resume… by using resume-helper"). Confidence: 0.75
- When given a folder of inputs (e.g., a folder of job descriptions), wants all of them processed one-by-one in a batch — not a sample — with each deliverable written to a dedicated output folder (one subfolder per input); re-confirmed when asking for resumes "one by one in the output folder". Confidence: 0.8
- Wants every output deliverable produced in multiple formats, all kept alongside each other in the same folder: after the primary `.docx`, asked for plain-text `.txt` versions ("all files of output folder .docx file should display in text editor"), then Markdown `.md` versions with proper formatting ("make all .txt file in .md file of output folder"), then PDFs of every `.md` ("make pdf for all .md files in output folder") — i.e., progressively requests the same content in text-editor-friendly AND send-ready formats. (Note: the `.txt`/`.md` copies are ultimately treated as intermediate — later asked to delete all `.md`/`.txt` from the output folder, keeping only final deliverables.) Confidence: 0.7
- Prefers using tooling already installed on the system (e.g., headless Chrome to convert Markdown → PDF) over installing new npm packages, and favors self-contained scripts with zero dependencies (wrote a minimal inline Markdown→HTML converter rather than relying on the `marked` package). Confidence: 0.75
- Keeps the project folder clean: cleans up temporary npm/install artifacts created during a task (removed `marked` install and reinstated `docx`) and does not leave scratch/temp files in the project (temp HTML files deleted after PDF generation). Confidence: 0.7
- Verifies batch outputs before reporting done — counts files per folder, checks non-empty sizes and magic bytes (e.g., `%PDF` header) to confirm every deliverable is valid. Confidence: 0.7
- When the user supplies missing personal details (e.g., real LinkedIn URL, a headshot photo), expects the agent to substitute them for placeholders and regenerate every deliverable format (.docx, .pdf, .txt, .md, spec) across all output folders — not leave fill-ins for the user to complete afterward. Confidence: 0.8
- Wants a professional headshot photo embedded in resume deliverables (top-right of the header) in both the .docx and .pdf whenever a photo is provided. Confidence: 0.7
- Keeps output folders tidy: on request, deletes all auxiliary text copies (`.md`, `.txt`) across every subfolder, leaving only the primary deliverables (`.docx`, `.pdf`, `spec.json`). Confidence: 0.9
- Prefers a consolidated, format-separated output organization for batch deliverables: all per-job JSON specs merged into a single `.specs` file, all `.docx` into a `docx/` subfolder, all `.pdf` into a `pdf/` subfolder, and empty per-job subfolders removed ("make one file .specs for all .json and do seperate all .docx and .pdf file from .specs"). Note: when moving same-named files (e.g., `resume.pdf`) into one folder, they must be given unique job-specific names to avoid overwrites. Confidence: 0.6
- Values being able to view file content in a plain text editor and asked to "make visible .docx file in text editor"; when a format is binary (e.g., .docx is a zip), the agent should explain the limitation and offer a text-viewable alternative rather than assuming the binary format is acceptable on its own. Confidence: 0.7
- Wants every resume deliverable tailored to its specific job description — the JD's exact job title in the header, the JD's verbs opening bullet points, and the JD's domain vocabulary in the summary — rather than one generic resume reused across jobs (asked to "make different different resume for given job description … after comparing resume and make according to given Job Descriptions"). Confidence: 0.9
- Values honesty in tailored resumes: when the candidate lacks a required skill (e.g., AEM, Temenos T24) or a claimed title (e.g., Test Lead), the agent should not fabricate it — instead flag the gap with bracketed placeholders and an explicit red note telling the user to fill in or delete. Confidence: 0.85
- Values detailed verification of every deliverable before reporting done: in addition to counts and file sizes, checks that each .docx/.pdf actually embeds the photo (magic bytes / media parts) and the LinkedIn link, and reports a pass/fail per item. Confidence: 0.75
- When an expected folder or file is missing at task start (e.g., an output folder deleted, files moved), the agent should search the workspace for them before rebuilding, and recover from what is found (e.g., reusing the moved resume/photo references). Confidence: 0.7
- For frontend builds, wants the latest, maintained, modern stack and techniques — explicitly asked to "follow latest features" and use "latest front end techniqur" (assistant picked Vite + React + TypeScript, @dnd-kit over the deprecated react-beautiful-dnd, idb, date-fns, lucide-react) — rather than older or unmaintained libraries. Confidence: 0.75
- When building an app or feature, expects the agent to locate and follow the provided reference/requirements document (e.g., a `Reference/prompt.md` in the project that specifies the feature set) rather than improvising the scope ("refer this prompt"). Reaffirmed strongly when the user said "use only prompt.md" and told the agent to strip extra features back to just the tracker. Confidence: 0.85
- Wants the app scope kept strictly minimal — only the feature explicitly requested (e.g., a plain "Job Tracker") with no extra invented pages, tabs, or fake navigation added unprompted; when such extras are added (an ATS Analyzer page, library manager, or mock nav like "Hiring Cockpit"), the user asks to remove them and show only the job tracker ("only job tracker should show other features no need"). Confidence: 0.85
- Does not want invented/marketing brand names applied to their app (asked not to use the "QAJobFit" title); prefers a simple, plain title reflecting what the app is (e.g., "Job Tracker") for the browser title, sidebar brand, and UI. Confidence: 0.8
- Wants built web apps to actually run on the local machine before being reported done ("run on the local") — i.e., start the dev server, confirm it serves on localhost, and exercise the flows in a browser, not just produce code that compiles. Confidence: 0.7
- When a design/reference image is attached to a build request, expects the delivered UI to follow that image's design language (e.g., matched the QAJobFit dashboard's orange accent, sidebar nav, dark hero card, stat cards) rather than inventing a different visual style. Confidence: 0.65
- Prefers deploying finished web apps to Vercel for production hosting (asked to "deploy it on the vercel" with a production build), and will supply a Vercel token for non-interactive CLI deployment; expect the deliverable to be a stable production URL, not just a locally running dev server. Confidence: 0.85
- When deploying a specific project/folder, wants each distinct app deployed to its own dedicated Vercel project and URL — not silently re-deployed under another project's link. The agent should verify the folder's `.vercel` link actually points to the intended destination (and to which project the token's account owns) before running `vercel --prod`, and flag/offer to fix when a stale link would deploy the wrong version. Confidence: 0.7
- Wants deployments to be scripted and reusable: when deploying a project, prefers a deployment script (e.g., a `deploy.bat` / shell script committed in the project root) that runs the build and `vercel --prod` for production, rather than relying on one-off ad-hoc commands; asked to "create a new deployment script to deploy on Vercel". Confidence: 0.75
